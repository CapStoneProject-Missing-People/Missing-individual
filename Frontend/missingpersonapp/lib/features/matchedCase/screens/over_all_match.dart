import 'package:flutter/material.dart';

class OverAllMatch extends StatelessWidget {
  const OverAllMatch({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Missing Person Over All Match Page'),
    );
  }
}